# Runtime dependencies. Pinned to compatible lower bounds; tested with the
# versions noted in README. Standard library covers everything else
# (sqlite3, http.server, csv, statistics, json, argparse, logging).
requests>=2.31
cryptography>=41.0
openpyxl>=3.1

# Test-only
pytest>=8.0
