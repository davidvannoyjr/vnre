# Secrets — never commit
*.local.md
**/fub-api-key*.md
**/fub-key*.md

# Tool / dependency noise
node_modules/

# Local skill configs (may hold keys / X-System secrets)
config.json
**/config.json
_data/
# Run-state ledgers (hold real CRM/financial counts) — keep the template, ignore live state
_state/
**/_state/

# Binaries that belong in Drive, not git
*.dmg
*.png
*.jpg
*.jpeg
*.skill

# Python cache
__pycache__/
*.pyc
